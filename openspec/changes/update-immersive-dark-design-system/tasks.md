## 1. 规范与基线

- [x] 1.1 在变更提案中将 `design-system` 的 Purpose、设计风格和 Requirements 定义为沉浸式暗色主视觉；正式 Spec 在变更归档时同步更新
- [x] 1.2 定义内容型 `immersive` 与工具型 `utility` 页面分类及适用边界
- [x] 1.3 建立现有页面与两种页面模式的迁移清单

## 2. Design Token 与主题

- [x] 2.1 建立暗色背景、文字、交互、语义色和图片遮罩的核心 Token，并迁移冲突别名调用；遗留亮色场景 Token 在页面迁移后按 6.4 清理
- [x] 2.2 收敛 Typography、Spacing、Radius、Shadow、Blur 与 Motion 的语义 Token
- [x] 2.3 统一 Cupertino 与页面实际使用的主题入口，明确 Material 控件的主题承载方式
- [x] 2.4 为 Token 和主题入口添加聚焦测试

## 3. 公共组件

- [x] 3.1 审计现有页面骨架、Section Header、网络图片、加载、错误、空态和操作组件，记录直接复用、扩展或保留局部实现的证据
- [x] 3.2 在至少两个代表场景中先组合验证沉浸式与工具型页面骨架，再将稳定结构晋升为公共组件
- [x] 3.3 在至少两个代表场景中验证 Hero 影像、渐变遮罩、叠图标题、指标组和毛玻璃操作，再为重复模式建立公共组件或受控变体
- [x] 3.4 优先扩展现有导航栏、Section Header、网络图片和异步状态组件，避免平行重建
- [x] 3.5 为每个新增或修改的公共组件先添加失败的 Widget 测试或契约测试，再实施组件

## 4. Agent 固定工作流

- [x] 4.1 在 Flutter 子项目 Agent 规则中加入 UI 开发前置检查和组件优先级
- [x] 4.2 要求 UI 提案和实现报告列出页面模式、复用组件、新增组件及新增 Token
- [x] 4.3 增加硬编码样式和重复组件模式的聚焦检查方式
- [x] 4.4 将设计规范检查纳入 UI 变更的完成条件

## 5. 代表页面验证

- [x] 5.1 以现有 Home 作为全屏封面式 Hero 的第一个基准场景
- [x] 5.2 选择 Route Discovery 或 Trip 作为第二个沉浸式场景，验证后再晋升稳定公共组件
- [x] 5.3 选择 Equipment 或 Settings 作为工具型暗色场景，并与第二个工具型页面共同验证页面骨架
- [x] 5.4 验证加载、失败、无图、长标题、大字体和小屏设备场景

## 6. 渐进迁移

- [x] 6.1 优先迁移 Trip 中的遗留亮色卡片和错误的 Sheet Token 使用
- [x] 6.2 迁移 Guide 的页面私有样式和操作组件
- [x] 6.3 统一普通页面导航、标题层级和异步状态
- [x] 6.4 审计并移除确认无调用的遗留 Token、重复组件和兼容别名

## 7. 验证

- [x] 7.1 运行受影响组件的 Widget 测试
- [x] 7.2 运行 `flutter analyze`
- [x] 7.3 运行 `flutter test`
- [ ] 7.4 在 iOS、Android 和 Web 代表尺寸上执行视觉与交互检查
- [ ] 7.5 记录未迁移页面和剩余视觉风险
