# Stitch Prompt — 首页（Home）

---

```
Design the Home screen for the Walk hiking app (iOS, Cupertino style).

Navigation bar: "首页" title, no action buttons.

Scrollable content, sections from top to bottom:

1. WEATHER CARD (full width, ~160px height)
   - Gradient background: warm orange-to-amber for sunny weather
   - Left side: greeting "早上好，Alex", location "杭州", temperature "18°C", condition "晴"
   - Right side: wind "3.2 m/s", humidity "65%"
   - Top-right corner: small "海拔" pill button (outlined, white text)
   - Bottom-right: small refresh icon

2. SECTION "我的行程" with "查看全部 ›" link (right-aligned)
   - Horizontal scroll of trip cards (~160px wide each)
   - Each card: green gradient background (#388E3C to #4CAF50), trip name bold white, date range small white, route name small white, person icon + count

3. SECTION "推荐路线" with "查看全部 ›" link
   - Horizontal scroll of route cards (~180px wide each)
   - Each card: cover photo top (16:9), route name bold, location with pin icon, difficulty badge (orange chip "中等"), distance + elevation metrics row

4. SECTION "徒步攻略" with "查看全部 ›" link
   - Horizontal scroll of guide cards (~160px wide each)
   - Each card: cover photo, title text below

Bottom tab bar: Home (active blue), Routes, center blue circle FAB with + icon, Gear, Profile.
```
