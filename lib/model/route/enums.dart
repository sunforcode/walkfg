/// 路线相关枚举类型

/// 路线类型枚举
enum RouteType {
  /// 环线
  circular,
  
  /// 单向
  oneWay,
  
  /// 往返
  roundTrip,
}

/// 路线方向枚举
enum RouteDirection {
  /// 顺时针
  clockwise,
  
  /// 逆时针
  counterClockwise,
}