# 环境变量模板
#
# 使用方式：复制为项目根目录的 .env 并填入真实值。
#   cp .env.example .env
# .env 被 gitignore，不会进入版本库。
# CI 构建时从 GitHub Secrets 注入（见 .github/workflows/ci.yml）。

# Mapbox 公开 access token（pk. 开头），3D 地图必需。
# 缺失时地图区域显示占位界面，其余功能不受影响。
MAPBOX_TOKEN=pk.YOUR_MAPBOX_PUBLIC_TOKEN
